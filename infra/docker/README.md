# Docker

Phase 0 uses the root `docker-compose.yml` for local dependencies.

Future artifacts may include:

- Backend production Dockerfile.
- Mobile build notes.
- Observability compose overrides.
- Deployment-specific container configuration.
